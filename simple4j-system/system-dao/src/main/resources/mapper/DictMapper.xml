<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple4j.user.mapper.DictMapper">

	<!-- 通用查询映射结果 -->
	<resultMap id="dictResultMap" type="com.simple4j.user.entity.Dict">
		<id column="id" property="id"/>
		<result column="parent_id" property="parentId"/>
		<result column="code" property="code"/>
		<result column="dict_key" property="dictKey"/>
		<result column="dict_value" property="dictValue"/>
		<result column="sort" property="sort"/>
		<result column="remark" property="remark"/>
		<result column="is_deleted" property="isDeleted"/>
	</resultMap>

	<!-- 通用查询结果列 -->
	<select id="getValue" resultType="java.lang.String">
        select
            dict_value
        from simple4j_dict where code = #{param1} and dict_key = #{param2} and is_deleted = 0 limit 1
    </select>

	<select id="getList" resultMap="dictResultMap">
        select code, dict_key, dict_value, sort, remark from simple4j_dict where code = #{param1} and dict_key >= 0 and is_deleted = 0
    </select>

	<!-- oracle 版本 -->
	<!--<select id="getValue" resultType="java.lang.String">
		select
			dict_value
		from simple4j_dict where code = #{param1, jdbcType=VARCHAR} and dict_key = #{param2} and dict_value >= 0 rownum 1
	</select>-->

	<select id="tree" resultType="com.simple4j.user.entity.Dict">
		select
		<include refid="baseColumnList"/>
		from simple4j_dict where is_deleted = 0
	</select>

	<sql id="baseColumnList">
        id, parent_id, code, dict_key, dict_value, sort, remark, is_deleted
    </sql>

</mapper>
